/**
 * Shared minimal utility types for the domain layer.
 * These types are intentionally small to keep the domain package focused
 * on business invariants rather than infrastructure concerns.
 */
export type Nullable<T> = T | null;
